Data :: struct
{
    texture: texture_id,
    sampler: sampler_id,
}

main :: (uv: vec2 @in_loc(0), data: ^Data @data) -> vec4 @out_loc(0)
{
    return texture_sample(data.texture, data.sampler, uv);
}
