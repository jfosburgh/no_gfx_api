
Vertex :: struct
{
    pos: vec4,
    color: vec4
}

Data :: struct
{
    verts: []Vertex,
}

Output :: struct
{
    pos: vec4 @position,
    color: vec4 @out_loc(0),
}

main :: (vert_id: uint @vert_id, data: ^Data @data) -> Output
{
    out: Output;
    out.pos = vec4(data.verts[vert_id].pos.xyz, 1.0);
    out.color = data.verts[vert_id].color;
    return out;
}
